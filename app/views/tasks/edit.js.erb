$(".edit-modal").html("<%= j(render 'edit') %>")
$('#datetimepicker').datetimepicker({format: 'YYYY/MM/DD HH:mm'});
var inst = $('[data-remodal-id=edit-modal]').remodal();
inst.open();
$(document).on('closed', '.remodal', function (e) {
  inst.destroy();
});
